import React from 'react';

export default function ExternalDisplay({ children }: { children?: React.ReactNode }) {
  return null;
}

export function useExternalDisplay(): Record<string, any> {
  return {};
}